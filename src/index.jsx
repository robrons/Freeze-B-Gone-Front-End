import React from 'react';
import { render } from 'react-dom'
import Home from 'Home.jsx'

render((<div>
        <ButtonAppBar/>
        <SimpleTabs/>
        <FloatingActionButton/>
        </div>), app);
